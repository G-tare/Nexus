# Landing Page — Environment Variables
# Copy this to .env.local and fill in

# URL of the web dashboard (where the "Dashboard" button links to)
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000/servers
